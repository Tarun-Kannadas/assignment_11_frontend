# 💼 Resume Frontend (React + Vite)

This is the **frontend** for the Resume Project built using **React (Vite)**.  
It fetches resume data (profile, education, experience, etc.) from the **Express + MongoDB backend API** and displays it beautifully.

---

## 🚀 Features

- React components for each resume section
- Fetches live data from backend using `fetch()`
- Displays data in styled tables and cards
- Modular folder structure for scalability
- Responsive layout using CSS
- API integration from `http://localhost:5000/api/resume`

---
